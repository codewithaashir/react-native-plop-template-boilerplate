import { DEFAULT_{{upperCase name}} } from './{{pascalCase name}}.types';

export function default{{upperCase name}} (data) {
    return {
        type: DEFAULT_{{upperCase name}},
        data,
    };
}